TODO:

- Delete config1.h
- Delete unixres.cc
- Replace the extra fields in the monster with dynamic structures.

In obj name rename:

Rework ReallocateExtraObjectSpace to take a std::string instead of a ptr/length

Look at AttachMonsterIdToObject for simplification.
